export enum PaymentMethod {
  CASH = 'cash',
  DEBIT = 'debit',
  CREDIT = 'credit',
  QRIS = 'qris',
  TRANSFER = 'transfer',
}

export enum TransactionStatus {
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  REFUNDED = 'refunded',
}
